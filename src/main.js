import React from "react";
import ReactDOM from "react-dom/client";
import StudMetricsWebsite from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <React.StrictMode>
    <StudMetricsWebsite />
  </React.StrictMode>
);
